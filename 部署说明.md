# 🚀 部署到 GitHub Pages 说明

> 作用：把工作台发布到网上一个固定网址，**MacBook 和 iPad 都用浏览器打开同一个网址**，配合「云端同步」后，家长在电脑上改、孩子在平板上看，随时随地（有网就行）。
> 本文件夹里已经放好了要发布的内容，你只需要：**① 申请 GitHub 账号 → ② 双击一键部署脚本**（约 5 分钟）。

---

## 一、如何申请 GitHub 账号（免费，第一次用的话）

1. 打开浏览器访问 **https://github.com/** → 点右上角 **Sign up（注册）**。
2. 依次填写：
   - **Email**：填写常用邮箱（163 / QQ / Gmail 均可）。
   - **Password**：至少 8 位，建议用密码管理器生成并保存。
   - **Username（用户名）**：**全球唯一**，会显示在网址里，建议拼音+数字，例如 `xiaoya-mama2026`（想好再填，以后可改）。
3. 完成拼图验证（可能会让你把图形拖到正确位置）。
4. 提交后，GitHub 会给你填的邮箱发一封**验证邮件** → 打开邮件点 **Verify email address** 完成验证。
   > ⚠️ 不验证邮箱的话无法创建仓库，这步一定要做。
5. 回到页面按引导选择：账号类型选 **Free（免费）** 即可，其他问题随意选。
6. （建议）开启二次验证：登录后点右上角头像 → **Settings → Password and authentication → Enable two-factor authentication**，用手机验证 App 扫码，更安全。

---

## 二、一键部署（推荐）

1. 双击本文件夹里的 **`一键部署.command`**（MacBook 上）。
   - 若提示"无法打开"，右键该文件 → **打开**，或打开「终端」运行：`bash 一键部署.command`
2. 脚本会自动：
   - 检查 git；
   - **如果电脑没有 GitHub 工具(gh)，会自动下载独立版**（约14MB，只需一次，装到 `~/.workbench-gh`，不需要 Homebrew）；
   - **首次运行会引导你登录 GitHub**（选 GitHub.com → HTTPS → Login with a web browser → 复制授权码 → 浏览器打开 https://github.com/login/device 粘贴授权）；
   - 自动创建公开仓库 `study-workbench`、上传文件、开启 GitHub Pages。
3. 完成后终端会显示网址：**`https://你的用户名.github.io/study-workbench/`**。
4. 等 1~2 分钟，用 MacBook 和 iPad 的 Safari 打开该网址即可使用。
   - iPad 上建议：Safari 打开 → 点分享按钮 → **添加到主屏幕**，像 App 一样用。

> 说明：免费 GitHub 账号的 Pages 只能使用**公开仓库**。这个仓库里只有工作台程序代码（不含任何家庭数据，家庭数据都在 Firebase 云端/设备本地），公开没有隐私风险；如果你介意，可后续升级付费或换其他托管。

---

## 三、手动部署（不想装 GitHub 工具时，备选）

1. 在 GitHub 网页上点 **+ → New repository**，仓库名填 `study-workbench`，选 **Public**，创建。
2. 本文件夹内操作：打开「终端」→ `cd` 到本文件夹 → 依次运行：
   ```bash
   git init
   git branch -M main
   git add -A
   git commit -m "部署工作台"
   git remote add origin https://github.com/你的用户名/study-workbench.git
   git push -u origin main
   ```
   （第一次 push 会要求输入 GitHub 用户名和密码；密码处填 **Personal access token**：Settings → Developer settings → Personal access tokens → Generate new token，勾选 `repo` 权限）
3. 打开仓库页面 → **Settings → Pages** → Source 选 **Deploy from a branch** → Branch 选 `main` / `/(root)` → **Save**。
4. 等 1~2 分钟，访问 `https://你的用户名.github.io/study-workbench/`。

---

## 四、以后怎么更新工作台

- 我修改程序后，会同时更新这里的 `index.html`。
- 你把新 `index.html` 复制覆盖进本文件夹，再**双击「一键部署.command」**即可，脚本会自动推送更新。

## 五、常见问题

- **打开网址是空白/404**：等 1~2 分钟再刷新；确认网址是 `https://用户名.github.io/study-workbench/`。
- **iPad 打不开**：确认有网络；iOS 有时需清一下 Safari 缓存。
- **想让家人也能访问**：把网址发给他们即可（公开网页，无需登录）。
- **想在孩子设备上也改数据**：需要访问家长后台（⚙️→1234），或直接让孩子只用来打卡。

---

## 六、一键部署失败怎么办（排错）

脚本运行时会自动把全过程写入同文件夹的 **`部署日志.txt`**。如果失败，请按下面顺序排查：

1. **看终端里的红色 ❌ 提示**，它已说明是哪一步失败（网络 / git / gh / 登录 / 上传）。
2. **复制 `部署日志.txt` 的内容发给我**，我直接帮你看具体原因。
3. 常见原因与解决：
   - **无法访问 github.com**：检查网络/代理后重试。
   - **自动下载 gh 失败 / 提示版本获取失败**：一般是网络问题，检查网络后重试；仍不行就改用「三、手动部署」。
   - **登录后仍失败 / 获取用户名失败**：很可能是**注册后没去邮箱点 GitHub 验证邮件** → 去邮箱完成验证后再运行。
   - **创建仓库失败**：用户名已被占用、或仓库 `study-workbench` 已存在（重跑即可，脚本会自动更新）。
   - **双击没反应**：右键 `一键部署.command` → 打开；或在「终端」里运行 `bash 一键部署.command`。
4. 实在不行，用本说明「三、手动部署」的网页方式也能完成，不需要任何命令行工具。
