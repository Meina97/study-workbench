# ☁️ 云端同步设置说明（Supabase 账号登录版，大陆无需代理，一次性约 12 分钟）

> 目的：家长在 MacBook 上录入/修改教辅、课程，孩子 iPad / 苹果台式机打开同一工作台即可**自动实时同步**（每 4 秒一次），无需导出/导入。
> 安全：数据放在 **Supabase（新加坡节点）**，只有登录"家庭账号"后才能真正读写；**邮箱/密码只保存在各设备本地，不会上传云端**。
> 不需要 Google/Firebase 账号，用邮箱或 GitHub 账号即可。

---

## 第 1 步：注册并登录 Supabase

1. 打开 **https://supabase.com** → **Start your project**。
2. 用 **GitHub 账号**（Meina97）或邮箱注册登录，验证邮箱。

## 第 2 步：创建项目（免费）

1. **New project**：
   - **Name**：随意，如 `workbench`
   - **Database Password**：自己设一个并**记下来**（工作台用不到，管理用）
   - **Region**：选 **Southeast Asia (Singapore)**
2. 点 **Create new project**，等 1~2 分钟。

## 第 3 步：创建数据表 + 安全规则（复制粘贴即可）

1. 左侧 **SQL Editor → New query**，粘贴并 **Run**：

```sql
create table if not exists workbench (
  room text primary key,
  data text not null,
  updated_at bigint not null
);
alter table workbench enable row level security;
create policy "auth_access" on workbench for all
  using (auth.role() = 'authenticated')
  with check (auth.role() = 'authenticated');
```

2. 看到 `Success` 即完成。这条规则保证：**只有登录的家庭账号才能读写数据**（网页公开密钥无法访问）。

## 第 4 步：创建"家庭账号"（邮箱+密码）

1. 左侧 **Authentication → Providers（登录方式）** → 找到 **Email** → **启用（Enable）** → 保存。
2. 左侧 **Authentication → Users（用户）→ Add user（添加用户）**：
   - 邮箱：填一个家人的邮箱（如 `family@xxx.com`）
   - 密码：设一个 8 位以上密码
   - 点 **Create user**
3. **这个邮箱+密码就是"家庭账号"**，MacBook 和 iPad 两端都用它登录（不用注册新用户）。

## 第 5 步：拿到"网址"和"密钥"

左侧 **Project Settings → API**，复制：
- **Project URL**：形如 `https://xxxx.supabase.co`
- **anon public key**：`eyJhbGciOi...` 开头的一长串

## 第 6 步：在 MacBook 上启用同步

1. 打开工作台 → 右下角 **⚙️ 齿轮** → 密码 `1234` → **孩子信息** → 「☁️ 云端实时同步」。
2. 填写：
   - **家庭房间码**：如 `xiaoya2026`（两端必须一字不差）
   - **Supabase 项目网址**：第 5 步的 Project URL
   - **anon 公开密钥**：第 5 步的 anon key
   - **家庭账号邮箱**、**家庭账号密码**：第 4 步创建的那个
   - 勾选「启用云端同步」→ 点「**保存并连接**」
3. 状态变绿色「☁️ 已连接云端（房间：…）」即成功。

## 第 7 步：在 iPad / 苹果台式机上启用

1. 打开同一网址 **https://Meina97.github.io/study-workbench/**。
2. 同样进入家长后台 → 孩子信息 → 云端实时同步：填**完全相同的** 房间码/网址/密钥/邮箱/密码 → 启用 → 保存并连接。
3. 之后 MacBook 改任何内容，iPad **4 秒内自动更新**。

**推荐**：iPad Safari 打开后 → 分享 → **添加到主屏幕**，像 App 一样用。

---

## 安全说明与常见问题

- **安全模型**：anon 密钥是公开的（网页里本来就有），但**只有家庭账号登录后才能真正读写数据**；房间码只是把你们的数据和其他家庭隔开。邮箱/密码不会上传云端（程序已处理），只存在各自设备。
- **别把房间码告诉外人**，两端保持一致即可。
- **连接失败/红色提示**：检查网址/密钥/房间码/邮箱/密码是否填对；确认第 3 步 SQL 成功、第 4 步已创建用户并启用 Email 登录；确认设备有网。
- **网址要加 https:// 吗**：要，完整粘贴 `https://xxxx.supabase.co`。
- **费用**：Supabase 免费版够家庭用，不会扣费。
- **两边同时改**：以最后保存的一方为准，建议同一时间只在一端编辑。
- **不想用云了**：孩子信息 → 云端实时同步 → 取消勾选 → 保存并连接，恢复纯本地模式。
