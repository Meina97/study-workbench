# ☁️ 云端同步设置说明（Supabase 版，大陆无需代理，一次性约 10 分钟）

> 目的：家长在 MacBook 上录入/修改教辅、课程，孩子 iPad / 苹果台式机打开同一工作台即可**自动实时同步**（每 4 秒自动同步一次），无需导出/导入。
> 原理：工作台把数据存到 **Supabase**（新加坡节点，大陆可直连）的免费数据库，按「家庭房间码」区分你家与其他家庭的数据。
> 不需要 Google/Firebase 账号，用邮箱或 GitHub 账号即可。

---

## 第 1 步：注册并登录 Supabase

1. 浏览器打开 **https://supabase.com** → 点 **Start your project**。
2. 用 **GitHub 账号**（你已经有 Meina97 了）或邮箱注册登录，按提示验证邮箱。

## 第 2 步：创建项目（免费）

1. 登录后点 **New project**。
2. 填写：
   - **Name**：随意，如 `workbench`
   - **Database Password**：自己设一个（8位以上），**记下来**（以后管理用，工作台用不到）
   - **Region**：选 **Southeast Asia (Singapore)**（离我们最近，最快）
3. 点 **Create new project**，等待 1~2 分钟创建完成。

## 第 3 步：创建数据表（复制粘贴即可）

1. 左侧菜单点 **SQL Editor** → 点 **New query**。
2. 把下面这段**全部复制**粘贴进去 → 点 **Run**：

```sql
create table if not exists workbench (
  room text primary key,
  data text not null,
  updated_at bigint not null
);
alter table workbench enable row level security;
create policy "anon_all" on workbench for all using (true) with check (true);
```

3. 看到 `Success` 就完成了。

## 第 4 步：拿到"网址"和"密钥"

1. 左侧菜单 **Project Settings（项目设置）→ API**。
2. 复制这两样（都在 **Project keys / API keys** 区域）：
   - **Project URL**：形如 `https://xxxx.supabase.co`
   - **anon public key**：一长串以 `eyJhbGciOi...` 开头的字符串

## 第 5 步：在 MacBook 上启用同步

1. 打开工作台 → 右下角 **⚙️ 齿轮** → 输入密码 `1234` → **孩子信息**。
2. 找到「**☁️ 云端实时同步**」面板，填写：
   - **家庭房间码**：填一个只有你们知道的代号，如 `xiaoya2026`（两端必须一字不差）
   - **Supabase 项目网址**：第 4 步复制的 Project URL
   - **anon 公开密钥**：第 4 步复制的 anon key
   - 勾选「**启用云端同步**」→ 点「**保存并连接**」
3. 状态变为绿色「☁️ 已连接云端（房间：…）」即成功。

## 第 6 步：在 iPad / 苹果台式机上启用

1. 打开同一个工作台网址（https://Meina97.github.io/study-workbench/）。
2. 同样进入家长后台 → 孩子信息 → 云端实时同步：填**同一个房间码**、**同一个网址**、**同一个密钥** → 勾选启用 → 保存并连接。
3. 连接成功后，MacBook 上改任何内容，iPad 端 **4 秒内自动更新**。

**推荐**：iPad 上用 Safari 打开后 → 分享 → **添加到主屏幕**，就像装了一个 App。

---

## 常见问题

- **连接失败/红色提示**：检查 网址、密钥、房间码 是否填对；确认第 3 步的 SQL 已成功运行；确认设备有网。
- **网址要加 https:// 吗**：要，完整粘贴 `https://xxxx.supabase.co`。
- **anon 密钥公开安全吗**：安全。anon key 本来就是给网页用的公开密钥；你的数据由数据库规则保护（第 3 步已设置），家庭房间码是访问钥匙，别告诉外人即可。
- **费用**：Supabase 免费版（Free plan）够家庭使用，不会扣费；每月自动重置用量。
- **两边同时改会怎样**：以最后保存的一方为准；建议同一时间只在一端编辑。
- **不想用云了**：孩子信息 → 云端实时同步 → 取消勾选 → 保存并连接，恢复纯本地模式。
