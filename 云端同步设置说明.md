# ☁️ 云端同步设置说明（一次性，约 10 分钟）

> 目的：家长在 MacBook 上录入/修改教辅、课程，孩子 iPad / 苹果台式机打开同一工作台即可**自动实时同步**，无需导出/导入。
> 原理：工作台通过 Firebase 免费实时数据库，按「家庭房间码」把两端数据连通。家长和孩子各填一次即可。

---

## 第 1 步：创建 Firebase 项目（用家长自己的 Google 账号，免费）

1. 电脑浏览器打开 <https://console.firebase.google.com/>，用 Google 账号登录。
2. 点「**创建项目**」→ 项目名称随意（如 `xiaoya-workbench`）→ Google Analytics 可关闭 → 创建。

## 第 2 步：注册 Web 应用，拿到配置

1. 在项目首页点 **Web 图标（</>）** → 应用昵称随意（如 `workbench`）→ 注册。
2. 注册后会显示一段 **Firebase SDK 配置**，形如：

```json
{
  "apiKey": "AIzaSy...",
  "authDomain": "xiaoya-workbench.firebaseapp.com",
  "databaseURL": "https://xiaoya-workbench-default-rtdb.firebaseio.com",
  "projectId": "xiaoya-workbench",
  "storageBucket": "xiaoya-workbench.appspot.com",
  "messagingSenderId": "1234567890",
  "appId": "1:1234567890:web:abcdef"
}
```

> 复制整段（**把 databaseURL 也带上**，若没有可在第 4 步创建数据库后再回来复制）。这段是网页公开配置，可以放心粘贴。

## 第 3 步：开启匿名登录

1. 左侧菜单 **Build → Authentication → 登录方式（Sign-in method）**。
2. 找到「**匿名**」（Anonymous）→ 启用 → 保存。

## 第 4 步：创建实时数据库

1. 左侧菜单 **Build → Realtime Database → 创建数据库**。
2. 位置选离你近的（如 `asia-southeast1`）→ 安全规则选「**测试模式**」（家庭用够用；更安全可选用下面第 7 步的规则）。
3. 创建完成后，把页面上方的数据库 URL（`https://xxx-default-rtdb.firebaseio.com`）补进第 2 步的配置里（即 `databaseURL` 字段）。

## 第 5 步：在 MacBook 上启用同步

1. 打开工作台 → 右下角 **⚙️ 齿轮** → 输入密码 `1234` 进入家长后台 → **孩子信息**。
2. 找到「**☁️ 云端实时同步**」面板：
   - **家庭房间码**：填一个只有你们知道的代号，如 `xiaoya2026`（两端必须完全一样，含大小写）；
   - **Firebase 配置**：粘贴第 2 步复制的整段 JSON；
   - 勾选「**启用云端同步**」→ 点「**保存并连接**」。
3. 状态变为绿色「☁️ 已连接云端（房间：…）」即成功。

## 第 6 步：在 iPad / 苹果台式机上启用同步

1. 在孩子设备打开同一个工作台文件（或部署后的网址）。
2. 同样进入家长后台 → 孩子信息 → 云端实时同步：
   - 填**同一个房间码**、粘贴**同一份配置** → 勾选启用 → 保存并连接。
3. 连接成功后，MacBook 上录入教辅/课程，孩子端几秒内自动更新。

**推荐**：iPad 上用 Safari 打开后，点「分享 → 添加到主屏幕」，就像装了一个 App。

---

## 第 7 步（可选）更安全的数据规则

如果不想用「测试模式」，可在 Firebase 控制台 Realtime Database → 规则中粘贴：

```json
{
  "rules": {
    "rooms": {
      "$room": {
        ".read": "auth != null",
        ".write": "auth != null"
      }
    }
  }
}
```

（只允许已登录的匿名用户读写，普通家庭测试模式已足够。）

---

## 常见问题

- **连接失败/状态红色**：检查设备是否联网、配置 JSON 是否完整（尤其 `databaseURL`）、房间码是否一致、是否已开启匿名登录。
- **两边同时改会怎样**：以最后保存的一方为准；建议同一时间只在一端编辑。
- **不想用云了**：回到 孩子信息 → 云端实时同步，取消勾选 → 保存并连接即可恢复纯本地模式。
- **费用**：Firebase Spark 免费计划对家庭用量绰绰有余，不会扣费。
- **还能用导出/导入吗**：可以，作为手动备份手段保留在「📲 跨设备同步」面板里。
